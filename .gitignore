.DS_Store

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan
tfplan.binary
crash.log
crash.*.log

# Variable files that might contain secrets
*.tfvars
*.tfvars.json
!terraform.tfvars.example

# AWS
.aws/

# Evidence artifacts (produced by CI, never committed)
evidence.tar.gz
evidence.tar.gz.sha256
evidence.sig.bundle
